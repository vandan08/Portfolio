import type { Metadata } from "next";
import Link from "next/link";
import { AIML_URL, LAB_URL } from "@/lib/links";

export const metadata: Metadata = {
    title: "Experiments — Vandan Sheth",
    description:
        "Small self-contained experiments, each on its own deployment: The Lab (an algorithm visualiser) and an AI/ML concept sandbox. Sketches, not products.",
};

interface Experiment {
    number: string;
    title: string;
    kicker: string;
    href: string;
    /** Shown as the visible link text — the subdomain, so it reads as a real address. */
    domain: string;
    description: string;
    notes: string[];
    stack: string;
}

const experiments: Experiment[] = [
    {
        number: "01",
        title: "The Lab",
        kicker: "Vandan's Intelligence · graph traversal",
        href: LAB_URL,
        domain: "lab.vandansheth.in",
        description:
            "Type a problem and watch an algorithm think. A breadth-first / depth-first traversal laid out like a page from a technical journal — a live diagram beside a monospace state ledger, the frontier expanding one ring at a time.",
        notes: [
            "Every step is generated by the real algorithm, so the animation is correct by construction — not a model guessing at it",
            "A classification layer turns a plain-English problem into the graph the engine runs on",
        ],
        stack: "Next.js  ·  TypeScript  ·  SVG",
    },
    {
        number: "02",
        title: "AI/ML Concepts",
        kicker: "A sandbox for the fundamentals",
        href: AIML_URL,
        domain: "aiml.vandansheth.in",
        description:
            "A place to take machine-learning ideas apart and see them run. Concepts built from first principles and rendered interactively, so the mechanics are visible rather than described.",
        notes: [
            "Built to understand the internals, not to wrap an API",
            "Grows one concept at a time — treat it as a notebook that happens to be deployed",
        ],
        stack: "Python  ·  TypeScript  ·  Next.js",
    },
];

export default function ExperimentsPage() {
    return (
        <>
            {/* Slim, self-contained top bar — no dependency on the portfolio nav. */}
            <header className="border-b border-rule bg-paper">
                <div className="container-page flex items-center justify-between py-4">
                    <Link
                        href="/"
                        className="font-display text-lg font-semibold tracking-tight text-ink hover:text-accent transition-colors"
                    >
                        Vandan Sheth
                    </Link>
                    <Link href="/" className="eyebrow transition-colors hover:text-accent">
                        ← Back to portfolio
                    </Link>
                </div>
            </header>

            <main className="py-16 md:py-24">
                <div className="container-page">
                    <p className="eyebrow mb-4">Experiments</p>
                    <h1 className="font-display text-4xl md:text-6xl font-medium tracking-tight mb-5">
                        Things built to{" "}
                        <em className="italic text-accent font-normal">find out</em>
                    </h1>
                    <p className="max-w-2xl text-lg text-ink-soft">
                        Each of these lives on its own subdomain and runs on its own. They
                        exist because a question was more interesting than the answer I had.
                    </p>

                    {/* The honest caveat, set as a marginal note rather than a disclaimer banner. */}
                    <p className="mt-8 max-w-2xl border-l-2 border-accent pl-4 text-sm italic text-ink-soft">
                        A note: these are experiments, not products. They are small, narrow
                        by design, and not built to production scale — expect rough edges,
                        and read them as sketches rather than finished work.
                    </p>

                    <div className="mt-14 md:mt-16 border-t border-rule">
                        {experiments.map((experiment) => (
                            <article
                                key={experiment.title}
                                className="group grid md:grid-cols-12 gap-4 md:gap-8 py-10 border-b border-rule"
                            >
                                <div className="md:col-span-2">
                                    <span className="font-display text-4xl md:text-5xl text-ink-faint group-hover:text-accent transition-colors duration-300">
                                        {experiment.number}
                                    </span>
                                </div>

                                <div className="md:col-span-10">
                                    <p className="font-mono text-[12px] text-ink-soft mb-3">
                                        {experiment.kicker}
                                    </p>

                                    <div className="flex flex-wrap items-baseline gap-x-5 gap-y-2 mb-3">
                                        <h2 className="font-display text-3xl md:text-4xl font-medium tracking-tight">
                                            {experiment.title}
                                        </h2>
                                        <a
                                            href={experiment.href}
                                            target="_blank"
                                            rel="noopener noreferrer"
                                            className="eyebrow no-underline text-accent transition-colors hover:text-ink"
                                        >
                                            {experiment.domain} ↗
                                        </a>
                                    </div>

                                    <p className="text-lg text-ink-soft max-w-2xl mb-6">
                                        {experiment.description}
                                    </p>

                                    <ul className="space-y-2 mb-6 max-w-2xl">
                                        {experiment.notes.map((note) => (
                                            <li key={note} className="text-ink-soft flex gap-3">
                                                <span className="text-accent select-none" aria-hidden>
                                                    —
                                                </span>
                                                <span>{note}</span>
                                            </li>
                                        ))}
                                    </ul>

                                    <p className="eyebrow">{experiment.stack}</p>
                                </div>
                            </article>
                        ))}
                    </div>
                </div>
            </main>

            <footer className="border-t border-rule py-10">
                <div className="container-page flex items-center justify-between">
                    <p className="eyebrow text-ink-faint">Experiments · Vandan Sheth</p>
                    <Link href="/" className="eyebrow transition-colors hover:text-accent">
                        ← Back to portfolio
                    </Link>
                </div>
            </footer>
        </>
    );
}
