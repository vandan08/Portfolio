/**
 * The experiments are their own projects with their own deployments, served
 * from subdomains. Kept here so the nav, the homepage teaser and the
 * /experiments page can never drift apart.
 */
export const LAB_URL = "https://lab.vandansheth.in";
export const AIML_URL = "https://aiml.vandansheth.in";
export const EXPERIMENTS_PATH = "/experiments";
